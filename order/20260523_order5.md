以下実装・修正を頼む
・レベルアップ簡易選択時のアイコン表示
    ・アイコン表示エリアが現在速度に被っているので、もう少し下に移動
    ・アイコンはリソースがないので、ベクターグラフィックで用意する。
        ・アイコンは正方形の丸角。
        ・中に[バルカン(改行)連射][バルカン(改行)弾数][スラスター(改行)速度]などテキストを書いて表現
            ・フォントサイズはアイコンをはみ出さない程度の大きさにする
        ・アイコン左上に [Q] など対応するキーを表示
            ・表示順は左から[Q][E][R]とする
        ・レベルアップ簡易選択時は、対応するキーは他機能を上書きし排他制御。Eミサイル発射不可、等
・デバッグ機能の追加、修正
    ・5Key 機能を少し変更、EXPを足すだけではなく、レベルアップしてストックを1追加
・タイトル画面のブラックフェードアウト時、タイトル文字、背景の星描画、フレア描画も共にフェードアウトする。

